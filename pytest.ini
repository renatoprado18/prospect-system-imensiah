[pytest]
# A suite mora em tests/. Sem esta linha o pytest varre a raiz inteira e coleta
# scripts/test_*.py — que NAO sao testes: sao smoke scripts manuais, com main() e
# sys.exit(). Coletar um deles ja custou a suite inteira: em 22/08/26 o
# scripts/test_cos_sensor.py importava services.cos_sensor, removido em 12/07
# (6713cec, sunset da Tonha-H), e o ImportError INTERROMPIA a coleta — 1.327
# testes verdes nao rodavam por causa de um orfao que ninguem consultava.
# Restringir o testpaths conserta a classe, nao a ocorrencia: os outros 4
# scripts/test_*.py seguem no repo e nenhum volta a derrubar a suite.
testpaths = tests
