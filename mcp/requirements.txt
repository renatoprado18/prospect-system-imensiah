# CoPiloto MCP server — deps
mcp>=1.28,<2          # SDK MCP oficial (FastMCP + stdio + streamable-http). 1.28 = testado.
psycopg2-binary>=2.9  # conexao Postgres propria (nao usa o db.py do INTEL)
httpx>=0.27           # chamada Voyage embeddings (search_memories semantica)
# uvicorn + starlette ja vem como deps do mcp[cli]; pinamos explicito p/ o
# transporte HTTP (http_server.py) nao depender de resolucao transitiva:
uvicorn>=0.30         # ASGI server do http_server.py
starlette>=0.37       # middleware de auth (BearerAuthMiddleware) + app streamable-http
