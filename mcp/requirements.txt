# CoPiloto MCP server — deps
mcp>=1.2.0            # SDK MCP oficial (FastMCP + transporte stdio)
psycopg2-binary>=2.9  # conexao Postgres propria (nao usa o db.py do INTEL)
httpx>=0.27           # chamada Voyage embeddings (search_memories semantica)
